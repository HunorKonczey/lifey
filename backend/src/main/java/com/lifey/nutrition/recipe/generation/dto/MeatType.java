package com.lifey.nutrition.recipe.generation.dto;

public enum MeatType {
    CHICKEN,
    BEEF,
    PORK,
    TURKEY,
    FISH,
    /** "Surprise me" — the model picks whatever suits the recipe. */
    ANY
}
