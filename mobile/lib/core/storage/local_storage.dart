/// Abstraction over the local offline-first storage.
/// Implementation deferred until the offline feature (pick an AGP-8-compatible
/// package — Isar 3.1.0 was dropped for breaking Android builds).
abstract interface class LocalStorage {
  // Local persistence operations go here.
}
